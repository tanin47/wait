services:
  - type: web
    name: wait
    plan: free
    runtime: image
    image:
      url: docker.io/tanin47/wait:0.1.0
    dockerCommand: java -jar wait-0.1.0.jar
    healthCheckPath: /healthcheck
    envVars:
      - key: PORT
        value: 9090
    autoDeploy: true
